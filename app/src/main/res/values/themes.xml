<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MemCards" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MemCards" parent="Base.Theme.MemCards">
        <item name="android:textViewStyle">@style/MemCardTextView</item>
    </style>
    <style name="MemCardTextView" parent="android:Widget.TextView">
        <item name="android:textSize">30sp</item>
        <item name="android:layout_margin">1dp</item>
        <item name="android:padding">10dp</item>
    </style>
</resources>